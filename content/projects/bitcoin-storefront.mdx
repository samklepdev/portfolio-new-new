---
slug: bitcoin-storefront
---

**PLACEHOLDER — replace with the real case study.** This file exists to exercise
the MDX pipeline and the prose styles; it is not content. Every other field on
this page (title, summary, role, tags, links) comes from Postgres, not from here.

## A second-level heading

Body copy renders at 1.7 line height against the dark surface. Inline elements:
a [link](https://example.com), some `inline code`, and **bold text**.

### A third-level heading

- A list item
- Another list item

> A blockquote, used sparingly.

```ts
// A fenced code block. Syntax highlighting is roadmap item 5.
export const revalidate = 60;
```
